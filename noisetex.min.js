
var addNoise;if(typeof addNoise!=="function"){addNoise=function(element,size,alpha){"use strict";var noiseCanvas=document.createElement("canvas"),noiseCtx,pixelData,pixels,x,y,sx,sy,rand;size=size||100;alpha=alpha||0.1;if(alpha>1.0){alpha=1.0;}
if(!noiseCanvas.getContext||!element||!element.style){return;}
noiseCanvas.width=noiseCanvas.height=size;noiseCtx=noiseCanvas.getContext("2d");pixelData=noiseCtx.getImageData(0,0,size,size);pixels=pixelData.data;for(x=0,sx=pixels.length;x<sx;x+=4){pixels[x]=pixels[x+1]=pixels[x+2]=0;rand=Math.random();pixels[x+3]=(255*rand*rand*alpha)|0;}
noiseCtx.putImageData(pixelData,0,0);pixelData="url("+noiseCanvas.toDataURL()+")";element.style.backgroundImage=pixelData;return pixelData;};}